extends ./pug/templates/template.pug

block head 
  +head(["header", "refund", "consultation-form", "crumbs", "selector", "footer", "mobile-menu", "accordion-item", "catalog-menu"])
block header 
  +header().dark
  +mobile-menu()
  +catalog-menu()
block content
  +crumbs({items: [{title: 'Возврат и обмен', href: '#'}]})
  +refund()
block footer 
  +footer()
block modals 
  